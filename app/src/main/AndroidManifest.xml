<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Bluetooth Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <!-- Location Permissions:
         Chỉ khai báo ACCESS_FINE_LOCATION và ACCESS_COARSE_LOCATION (Foreground Location).
         Lý do không dùng ACCESS_BACKGROUND_LOCATION:
         1. Ứng dụng chỉ lấy vị trí tại thời điểm sự kiện Bluetooth xảy ra trong lúc BluetoothWatcherService
            (Foreground Service) đang chạy liên tục với notification hiển thị.
         2. Theo tiêu chuẩn Android (API 29+) và quy định Google Play, việc lấy vị trí bên trong một Foreground Service
            được định danh foregroundServiceType="location" được tính là Foreground Location Access.
         3. Do đó, quyền ACCESS_BACKGROUND_LOCATION ("Cho phép mọi lúc") là HOÀN TOÀN KHÔNG CẦN THIẾT, giúp bảo vệ
            quyền riêng tư người dùng và tránh thủ tục xét duyệt khắt khe/nguy cơ bị từ chối trên Google Play Store. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Notifications & Foreground Service -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".BtWatcherApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground Service for continuous Bluetooth & Location Tracking -->
        <service
            android:name=".service.BluetoothWatcherService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />

        <!-- Note: BluetoothEventReceiver is registered dynamically inside BluetoothWatcherService
             to comply with Android 8.0+ (API 26) background execution limits on implicit broadcasts
             and prevent duplicate event processing. Do not declare statically here. -->

        <!-- Boot Receiver to restart foreground service if enabled -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
